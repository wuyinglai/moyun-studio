# 墨韵 - 后端编码规范

## 一、概述

本文档定义墨韵后端代码的编码规范，所有开发人员必须遵守。

---

## 二、命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件名 | snake_case | `file_ops.py`, `llm_config.py` |
| 类名 | PascalCase | `LLMService`, `FileWatcher` |
| 函数/方法 | snake_case | `read_file()`, `count_tokens()` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRIES`, `DEFAULT_TIMEOUT` |
| 私有属性 | _前缀 | `_internal_state` |
| 实例变量 | snake_case | `self.task_queue` |
| 模块名 | snake_case | `utils`, `core` |

---

## 三、异步规范

### 3.1 基本原则

```python
# ✅ 正确：使用 async/await
async def read_file(self, path: str) -> str:
    async with aiofiles.open(path, 'r', encoding='utf-8') as f:
        return await f.read()

# ❌ 错误：禁止同步操作
def read_file(self, path: str) -> str:
    with open(path, 'r') as f:  # 禁止！
        return f.read()
```

### 3.2 异步函数定义

```python
# ✅ 必须使用 async def
async def fetch_data(self, url: str) -> dict:
    ...

# ❌ 不要混用
def fetch_data(self, url: str) -> dict:  # 禁止！
    ...
```

### 3.3 异步生成器

```python
# ✅ 流式输出使用异步生成器
async def stream_generate(
    self,
    messages: list[dict]
) -> AsyncGenerator[str, None]:
    async for chunk in self.client.stream(messages):
        yield chunk
```

---

## 四、类型注解

### 4.1 必须包含类型注解

```python
# ✅ 函数签名必须包含类型注解
async def render_prompt(
    self,
    template: str,
    variables: dict[str, Any]
) -> str:
    ...

# ❌ 禁止省略类型
async def render_prompt(self, template, variables):  # 禁止！
    ...
```

### 4.2 联合类型

```python
# ✅ 使用 | 表示联合类型（Python 3.10+）
def get_task(self, task_id: str) -> Task | None:
    ...

# ✅ 或使用 Optional
from typing import Optional
def get_task(self, task_id: str) -> Optional[Task]:
    ...
```

### 4.3 泛型注解

```python
# ✅ 列表类型
async def get_all(self) -> list[CharacterProfile]:
    ...

# ✅ 字典类型
async def get_batch(self) -> dict[str, ProjectMeta]:
    ...

# ✅ 异步生成器
async def stream_logs(self) -> AsyncGenerator[str, None]:
    ...
```

### 4.4 复杂类型

```python
# ✅ 使用 TypeAlias 定义复杂类型
from typing import TypeAlias

MessageList: TypeAlias = list[dict[str, str]]
FilePath: TypeAlias = str | Path

async def send_messages(self, messages: MessageList) -> None:
    ...
```

---

## 五、异常处理

### 5.1 自定义异常类

```python
# ✅ 定义清晰的自定义异常
class AppException(Exception):
    """应用异常基类"""
    def __init__(self, message: str, code: str):
        self.message = message
        self.code = code
        super().__init__(self.message)

class FileNotFoundError(AppException):
    """文件不存在"""
    def __init__(self, path: str):
        super().__init__(
            message=f"文件不存在: {path}",
            code="FILE_NOT_FOUND"
        )

class TemplateNotFoundError(AppException):
    """模板不存在"""
    def __init__(self, template: str):
        super().__init__(
            message=f"模板不存在: {template}",
            code="TEMPLATE_NOT_FOUND"
        )

class LLMError(AppException):
    """LLM调用错误"""
    def __init__(self, message: str, model: str | None = None):
        detail = f"{message} (model: {model})" if model else message
        super().__init__(
            message=f"LLM调用失败: {detail}",
            code="LLM_ERROR"
        )
```

### 5.2 异常捕获与重新抛出

```python
# ✅ 使用 try-except 并重新抛出有意义异常
async def read_file(self, path: str) -> str:
    try:
        async with aiofiles.open(path, 'r', encoding='utf-8') as f:
            return await f.read()
    except FileNotFoundError:
        raise FileNotFoundError(path)
    except PermissionError:
        raise AppException(
            message=f"权限不足: {path}",
            code="PERMISSION_DENIED"
        )
    except Exception as e:
        raise AppException(
            message=f"读取文件失败: {e}",
            code="FILE_READ_ERROR"
        )
```

### 5.3 异常日志

```python
# ✅ 记录异常堆栈
try:
    await self.process(data)
except Exception as e:
    logger.error(
        f"处理数据失败: {e}",
        exc_info=True,  # 包含完整堆栈
        extra={"task_id": self.task_id}
    )
    raise
```

---

## 六、日志规范

### 6.1 日志配置

```python
# ✅ 使用标准 logging
import logging

logger = logging.getLogger(__name__)

# ❌ 不要使用 print
print("debug")  # 禁止！
```

### 6.2 日志级别使用

```python
# DEBUG: 调试信息，生产环境不显示
logger.debug(f"加载模板: {template_path}")

# INFO: 操作成功确认
logger.info(f"任务完成: {task_id}, 耗时: {elapsed}s")

# WARNING: 警告信息，需要关注但不影响功能
logger.warning(f"Token数接近上限: {tokens}/{max_tokens}")

# ERROR: 错误信息，需要调查
logger.error(f"LLM调用失败: {e}", exc_info=True)

# CRITICAL: 严重错误
logger.critical("数据库连接完全失败")
```

### 6.3 结构化日志

```python
# ✅ 使用 extra 参数传递结构化数据
logger.info(
    "任务状态更新",
    extra={
        "task_id": task.id,
        "status": task.status,
        "progress": task.progress
    }
)

# ❌ 不要拼接字符串
logger.info(f"任务{task.id}状态变为{task.status}")  # 禁止！
```

---

## 七、配置管理

### 7.1 pydantic-settings 使用

```python
# ✅ 使用 pydantic-settings
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    """应用配置"""
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        env_prefix="MOYUN_",
        case_sensitive=False
    )

    # 必填项：无默认值
    llm_api_key: str

    # 可选项：有默认值
    llm_model: str = "gpt-4"
    workspace_path: str = "./workspace"
    max_retries: int = 3

    # 敏感字段
    @property
    def masked_api_key(self) -> str:
        """返回脱敏的 API Key"""
        if len(self.llm_api_key) > 8:
            return f"{self.llm_api_key[:4]}...{self.llm_api_key[-4:]}"
        return "***"

# ❌ 禁止使用 os.getenv
api_key = os.getenv("API_KEY")  # 禁止！
```

### 7.2 配置访问

```python
# ✅ 全局配置单例
from functools import lru_cache

@lru_cache
def get_settings() -> Settings:
    return Settings()

# 使用
settings = get_settings()
```

---

## 八、API路由规范

### 8.1 路由文件结构

```python
# api/projects.py
from fastapi import APIRouter, HTTPException, Depends
from pydantic import BaseModel, Field
from typing import Optional

router = APIRouter(prefix="/projects", tags=["项目管理"])

# ============ 请求/响应模型 ============

class CreateProjectRequest(BaseModel):
    """创建项目请求"""
    name: str = Field(..., min_length=1, max_length=100)
    genre: str
    theme: str
    scale: int = Field(default=50000, ge=10000, le=200000)

class ProjectResponse(BaseModel):
    """项目响应"""
    project_id: str
    name: str
    genre: str

# ============ 依赖注入 ============

async def get_project_service() -> ProjectService:
    """获取项目服务"""
    return ProjectService(file_service=app.state.file_service)

# ============ 路由定义 ============

@router.post("/", response_model=ProjectResponse, status_code=201)
async def create_project(
    req: CreateProjectRequest,
    service: ProjectService = Depends(get_project_service)
):
    """创建新项目

    创建一个新的小说项目，包含基础目录结构。

    Args:
        req: 创建项目请求参数
        service: 项目服务

    Returns:
        创建的项目信息

    Raises:
        HTTPException: 项目创建失败时抛出
    """
    try:
        project = await service.create(req)
        return ProjectResponse(
            project_id=project.id,
            name=project.name,
            genre=project.genre
        )
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

### 8.2 响应格式

```python
# ✅ 使用统一响应格式
from typing import Generic, TypeVar

T = TypeVar("T")

class ApiResponse(BaseModel, Generic[T]):
    """统一API响应"""
    success: bool
    data: T | None = None
    message: str | None = None
    error: dict | None = None

def success_response(data: Any = None, message: str = "操作成功") -> dict:
    return ApiResponse(success=True, data=data, message=message).model_dump()

def error_response(message: str, code: str, details: Any = None) -> dict:
    return ApiResponse(
        success=False,
        error={"code": code, "details": details}
    ).model_dump()
```

---

## 九、数据模型规范

### 9.1 Pydantic 模型定义

```python
# models/project.py
from pydantic import BaseModel, Field
from datetime import datetime
from typing import Optional

class ProjectMeta(BaseModel):
    """项目元数据"""

    project_id: str = Field(..., description="项目ID")
    name: str = Field(..., min_length=1, max_length=100, description="项目名称")
    genre: str = Field(..., description="题材类型")
    theme: str = Field(default="", description="核心主题")
    tone: str = Field(default="", description="作品基调")
    scale: int = Field(default=50000, ge=10000, le=200000, description="目标字数")
    pov: str = Field(default="第三人称", description="叙事视角")

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: Optional[datetime] = None

    class Config:
        json_schema_extra = {
            "example": {
                "project_id": "proj_001",
                "name": "我的玄幻小说",
                "genre": "玄幻",
                "theme": "友情与成长"
            }
        }
```

### 9.2 枚举类型

```python
# models/enums.py
from enum import Enum

class TaskStatus(str, Enum):
    """任务状态"""
    PENDING = "pending"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"

class CharacterRole(str, Enum):
    """角色定位"""
    PROTAGONIST = "protagonist"
    ANTAGONIST = "antagonist"
    SUPPORTING = "supporting"
```

---

## 十、依赖注入

### 10.1 服务依赖

```python
# ✅ 使用 FastAPI 依赖注入
from fastapi import Depends

async def get_llm_service() -> LLMService:
    return app.state.llm

async def get_file_service() -> FileService:
    return app.state.file_service

async def get_prompt_engine(
    file_service: FileService = Depends(get_file_service)
) -> PromptEngine:
    return PromptEngine(
        prompts_path=settings.prompts_path,
        file_service=file_service
    )

# ✅ 在路由中使用
@router.post("/generate")
async def generate_content(
    req: GenerateRequest,
    llm: LLMService = Depends(get_llm_service),
    engine: PromptEngine = Depends(get_prompt_engine)
):
    prompt = await engine.render(req.template, req.variables)
    return await llm.complete(prompt)
```

### 10.2 避免循环依赖

```python
# ❌ 避免循环依赖
# a.py
from .b import B
class A:
    def __init__(self):
        self.b = B()

# b.py
from .a import A  # 可能导致循环导入！
class B:
    def __init__(self):
        self.a = A()

# ✅ 使用延迟导入或依赖注入
class B:
    def __init__(self, a: "A" = None):  # 类型注解用字符串
        self.a = a
```

---

## 十一、代码组织

### 11.1 文件组织原则

```python
# ✅ 一个文件一个主类（如果类较大）
# core/file_ops.py
class FileService:
    """文件操作服务"""
    ...

# core/llm.py
class LLMService:
    """LLM调用服务"""
    ...

# ❌ 不要在一个文件里放多个不相关类
# bad.py
class FileService:
    ...
class LLMService:  # 禁止！
    ...
```

### 11.2 模块初始化

```python
# ✅ __init__.py 清晰导出
# core/__init__.py
from .llm import LLMService
from .file_ops import FileService
from .prompt_engine import PromptEngine

__all__ = [
    "LLMService",
    "FileService",
    "PromptEngine",
]
```

### 11.3 导入顺序

```python
# ✅ 导入顺序：标准库 → 第三方库 → 本地模块
import os
from pathlib import Path
from datetime import datetime
from typing import Any

import aiofiles
from fastapi import APIRouter
from pydantic import BaseModel

from models import ProjectMeta
from core import LLMService
```

---

## 十二、测试规范

### 12.1 测试文件结构

```
backend/tests/
├── __init__.py
├── conftest.py              # pytest fixtures
├── test_file_ops.py         # 一个测试文件对应一个被测模块
├── test_llm.py
├── test_prompt_engine.py
└── test_api/
    ├── __init__.py
    ├── test_projects.py
    └── test_generate.py
```

### 12.2 pytest fixtures

```python
# conftest.py
import pytest
from pathlib import Path
from backend.core.file_ops import FileService

@pytest.fixture
def workspace(tmp_path):
    """创建临时工作空间"""
    workspace = tmp_path / "workspace"
    workspace.mkdir()
    return workspace

@pytest.fixture
async def file_service(workspace):
    """创建文件服务实例"""
    return FileService(workspace)

@pytest.fixture
def sample_project(workspace):
    """创建示例项目"""
    project_dir = workspace / "test_project"
    project_dir.mkdir()
    return project_dir
```

### 12.3 测试编写

```python
# test_file_ops.py
import pytest

class TestFileService:
    """FileService 测试"""

    @pytest.mark.asyncio
    async def test_read_file_success(self, file_service, sample_project):
        """测试成功读取文件"""
        # Arrange
        test_file = sample_project / "test.txt"
        test_file.write_text("hello world")

        # Act
        content = await file_service.read_file("test_project/test.txt")

        # Assert
        assert content == "hello world"

    @pytest.mark.asyncio
    async def test_read_file_not_found(self, file_service):
        """测试文件不存在"""
        with pytest.raises(FileNotFoundError):
            await file_service.read_file("nonexistent.txt")

    @pytest.mark.asyncio
    async def test_write_file_with_frontmatter(self, file_service, sample_project):
        """测试写入带 frontmatter 的文件"""
        # Arrange
        content = "正文内容"
        frontmatter = {"title": "测试", "version": "1.0"}

        # Act
        await file_service.write_file(
            "test_project/test.md",
            content,
            frontmatter=frontmatter
        )

        # Assert
        result, meta = await file_service.read_file("test_project/test.md")
        assert result == content
        assert meta["title"] == "测试"
```

---

## 十三、文档字符串

### 13.1 类文档字符串

```python
class LLMService:
    """LLM调用服务

    封装LiteLLM调用，提供流式生成、重试机制等功能。

    Attributes:
        config: LLM配置
        client: LiteLLM客户端实例
        retry_policy: 重试策略配置

    Example:
        >>> service = LLMService(config)
        >>> async for chunk in service.complete(messages):
        ...     print(chunk, end="")
    """
```

### 13.2 函数文档字符串

```python
async def complete(
    self,
    messages: list[dict[str, str]],
    model: str | None = None,
    stream: bool = True,
    temperature: float = 0.7
) -> AsyncGenerator[str, None]:
    """流式生成文本

    根据对话历史生成回复，支持流式和非流式两种模式。

    Args:
        messages: 对话消息列表，每条消息包含 role 和 content
        model: 模型名称，None使用默认模型
        stream: 是否流式输出，默认True
        temperature: 温度参数，控制随机性

    Yields:
        生成的文本片段（流式模式）

    Returns:
        完整生成的文本（非流式模式）

    Raises:
        LLMError: LLM调用失败时抛出
        ValidationError: 参数验证失败时抛出

    Example:
        >>> messages = [{"role": "user", "content": "你好"}]
        >>> async for chunk in service.complete(messages):
        ...     print(chunk, end="")
        你好！有什么可以帮助你的吗？
    """
```

### 13.3 简洁注释

```python
# ✅ 使用简洁的注释说明复杂逻辑
def calculate_priority(task: Task) -> int:
    """计算任务优先级"""
    # 高优先级：用户直接触发的任务
    if task.source == "user":
        return 100

    # 中优先级：系统自动触发的任务
    if task.source == "system":
        return 50

    # 低优先级：后台维护任务
    return 10

# ❌ 不要写无意义的注释
def calculate_priority(task: Task) -> int:
    # 初始化优先级为0
    priority = 0  # 禁止！这是废话
```

---

## 十四、Git 提交规范

### 14.1 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 14.2 Type 类型

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug修复 |
| docs | 文档更新 |
| style | 代码格式（不影响功能） |
| refactor | 重构（不是新功能或bug修复） |
| test | 测试相关 |
| chore | 构建或辅助工具 |

### 14.3 示例

```
feat(api): 添加项目创建API

- 新增 POST /api/projects 接口
- 支持创建时自动生成目录结构
- 添加参数验证

Closes #123
```

---

## 十五、前端编码规范

### 15.1 概述

本部分规范适用于墨韵前端原型代码，采用**单 HTML 文件**开发模式：
- Vue 3（CDN 引入）
- 原生 JavaScript（ES6+）
- 内联 CSS + CSS 变量主题系统

> 当原型验证完成、需转向正式项目时，可按 Vue 3 + TypeScript + Vite 技术栈重构。

### 15.2 文件结构

```
/d/newmoyun/
├── prototype.html          # 单 HTML 原型入口
├── frontend/               # 正式项目（未来）
│   ├── src/
│   └── vite.config.ts
└── docs/
    └── 编码规范.md
```

### 15.3 HTML 文件结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>墨韵 - AI小说创作助手</title>

    <!-- CDN 依赖（按需引入） -->
    <script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* ================================================
           全局样式与主题系统
           ================================================ */
        :root {
            /* 深邃夜紫（默认） */
            --bg-primary: #1a1a2e;
            --accent-primary: #3b82f6;
            /* ... */
        }

        /* 组件样式 */
        .component-name { /* ... */ }
    </style>
</head>
<body>
    <div id="app">
        <!-- 模板内容 -->
    </div>

    <script>
        const { createApp, ref, computed, watch, onMounted } = Vue;

        createApp({
            setup() {
                /* 逻辑代码 */
            }
        }).mount('#app');
    </script>
</body>
</html>
```

### 15.4 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| HTML 标签 | kebab-case | `file-tree`, `chat-panel` |
| CSS 类名 | kebab-case | `project-card`, `btn-primary` |
| JavaScript 函数 | camelCase | `loadFileTree`, `saveContent` |
| JavaScript 变量 | camelCase | `currentProject`, `isLoading` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRIES`, `API_BASE_URL` |
| 组件区块注释 | 3 等分 === | `/* ============ 状态 ============ */` |

### 15.5 JavaScript 规范（Vue 3 Composition API）

#### 15.5.1 setup() 函数分区

```javascript
createApp({
    setup() {
        // ================================================
        // 状态（ref）
        // ================================================
        const currentProject = ref(null);
        const loading = ref(false);

        // ================================================
        // 计算属性
        // ================================================
        const projectName = computed(() => currentProject.value?.name || '');

        // ================================================
        // 方法
        // ================================================
        async function loadProject() { /* ... */ }

        // ================================================
        // 生命周期
        // ================================================
        onMounted(() => { /* ... */ });

        // ================================================
        // 返回（暴露给模板）
        // ================================================
        return {
            currentProject,
            loading,
            projectName,
            loadProject,
        };
    }
});
```

#### 15.5.2 API 封装

```javascript
// ✅ 统一的 API 封装
async function api(url, options = {}) {
    const baseUrl = 'http://localhost:8000/api';
    try {
        const response = await fetch(baseUrl + url, {
            headers: { 'Content-Type': 'application/json' },
            ...options,
        });
        if (!response.ok) {
            const err = await response.json().catch(() => ({}));
            throw new Error(err.message || `HTTP ${response.status}`);
        }
        return response.json();
    } catch (e) {
        console.error('API Error:', e);
        throw e;
    }
}

// ✅ 使用
const projects = await api('/projects');
await api('/projects', {
    method: 'POST',
    body: JSON.stringify({ name: '新项目' })
});
```

#### 15.5.3 状态管理

```javascript
// ✅ 使用 ref 替代 Vuex/Pinia（原型阶段）
const store = ref({
    currentProject: null,
    projectList: [],
    settings: { theme: 'purple' },
});

// ✅ 访问 store.value.currentProject
```

### 15.6 CSS 规范

#### 15.6.1 CSS 变量（主题系统）

```css
/* ✅ 定义主题变量 */
:root {
    --bg-primary: #1a1a2e;
    --bg-secondary: #16213e;
    --bg-card: #0f3460;
    --accent-primary: #3b82f6;
    --text-primary: #e2e8f0;
    --text-secondary: #94a3b8;
    --border-color: #334155;
}

/* ✅ 主题切换 */
[data-theme="green"] {
    --bg-primary: #1a1f1a;
    --accent-primary: #6ee7b7;
    --text-primary: #e8f5e9;
}

/* ✅ 使用变量 */
.toolbar {
    background: var(--bg-secondary);
    color: var(--text-primary);
    border-bottom: 1px solid var(--border-color);
}
```

#### 15.6.2 类名规范

```css
/* ✅ BEM 风格（可选） */
/* block */
.btn { /* ... */ }
/* element */
.btn__icon { /* ... */ }
/* modifier */
.btn--primary { /* ... */ }

/* ✅ 统一前缀 */
.modal-xxx    /* 模态框 */
.toolbar-xxx  /* 工具栏 */
.panel-xxx   /* 面板 */
.tree-xxx    /* 文件树 */
.chat-xxx    /* 聊天 */
```

### 15.7 HTML 模板规范

#### 15.7.1 结构组织

```html
<div id="app">
    <!-- ========================================
         模态框（放顶层）
         ======================================== -->
    <div v-if="modalType === 'newProject'" class="modal">
        <!-- 新建项目表单 -->
    </div>

    <!-- ========================================
         主布局
         ======================================== -->
    <div class="app-container">
        <!-- 顶部工具栏 M01 -->
        <div class="toolbar">...</div>

        <!-- 主体 -->
        <div class="main-content">
            <!-- 左侧文件树 M02 -->
            <div class="sidebar-left">...</div>

            <!-- 中间编辑器 M03 -->
            <div class="editor-area">...</div>

            <!-- 右侧面板 M04 -->
            <div class="sidebar-right">...</div>
        </div>
    </div>
</div>
```

#### 15.7.2 Vue 指令使用

```html
<!-- ✅ v-if/v-show 区分使用 -->
<div v-if="loading">加载中...</div>        <!-- 移除 DOM -->
<div v-show="!loading">内容</div>         <!-- 显示/隐藏 -->

<!-- ✅ v-for 必须加 key -->
<div v-for="tab in tabs" :key="tab.path">...</div>

<!-- ✅ 事件绑定简写 -->
<button @click="handleSave">保存</button>
<input @input="onChange" v-model="content">

<!-- ✅ 类名绑定 -->
<div :class="['tab', { 'tab--active': isActive }]">
```

### 15.8 错误处理规范

```javascript
// ✅ 统一的错误提示
function showNotification(message, type = 'success') {
    notification.value = { show: true, type, message };
    setTimeout(() => { notification.value.show = false; }, 3000);
}

// ✅ 使用
try {
    await api('/projects', { method: 'POST', body });
    showNotification('创建成功');
} catch (e) {
    showNotification('创建失败: ' + e.message, 'error');
}
```

### 15.9 CDN 引入规范

```html
<!-- Vue 3 -->
<script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>

<!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<!-- Ant Design Vue（可选，如需更丰富组件） -->
<script src="https://unpkg.com/ant-design-vue@4.x/dist/antd.min.js"></script>
<link rel="stylesheet" href="https://unpkg.com/ant-design-vue@4.x/dist/reset.css">
```

### 15.10 开发流程

```
1. 原型阶段（当前）
   prototype.html → 功能验证 → 迭代修改

2. 重构阶段（未来）
   将 prototype.html 拆分为 Vue 3 组件项目
   按前端编码规范（Vue 3 + TypeScript）重构
```

### 15.11 Git 提交规范（前端原型）

```
feat(ui): 添加文件树拖拽功能
fix(bug): 修复主题切换闪烁问题
refactor(code): 重构 API 封装逻辑
style(css): 调整工具栏高度
docs: 更新原型说明
```

---

## 版本记录

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| v1 | 2026-05-11 | 初始版本 |
| v1.1 | 2026-05-11 | 新增前端编码规范（第十五章） |
| v1.2 | 2026-05-11 | 修改前端规范为单HTML原型模式 |
