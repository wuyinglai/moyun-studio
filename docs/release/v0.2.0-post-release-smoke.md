# v0.2.0 Post-release Checkout Smoke

Date: 2026-06-16

Mode: Tag Checkout Smoke + Release Verification

Risk: Risk B / Post-release Verification

## Summary

The `v0.2.0` developer preview release was verified from the published tag. The tag checks out cleanly, points to the expected release-ready commit, contains the expected release documents, and passes the required lightweight backend and frontend smoke checks.

Final result:

```text
v0.2.0 post-release checkout smoke passed.
```

## Current Main Commit

Main after returning from the tag:

```text
600969f docs: add v0.2.0 release report
```

This is expected. The release report commit is post-release documentation and is not part of the `v0.2.0` tag.

## Tag Verification

Local tag:

```text
v0.2.0
```

Checkout result:

```text
HEAD is now at 6ef7d78 docs: add T9.1 LLM smoke recheck
```

Expected tag commit:

```text
6ef7d7893e22a93141ac5c23eb2053bd1d7877b4
```

Detached HEAD during tag checkout was expected and normal.

## Remote Tag Verification

Remote tag entries:

```text
0ec1bbf304753a09c97f7c497240b1bad13d0f72 refs/tags/v0.2.0
6ef7d7893e22a93141ac5c23eb2053bd1d7877b4 refs/tags/v0.2.0^{}
```

The first hash is the annotated tag object. The peeled tag commit is the expected release-ready commit.

## GitHub Release Verification

GitHub Release:

[https://github.com/wuyinglai/moyun-studio/releases/tag/v0.2.0](https://github.com/wuyinglai/moyun-studio/releases/tag/v0.2.0)

Verified fields:

| Field | Value |
| --- | --- |
| Tag | `v0.2.0` |
| Title | `v0.2.0 Writing Quality Loop Developer Preview` |
| Prerelease | `true` |
| URL | `https://github.com/wuyinglai/moyun-studio/releases/tag/v0.2.0` |
| Published at | `2026-06-16T03:43:24Z` |

## Tag Document Check

The following files exist under the `v0.2.0` tag:

- `README.md`
- `CHANGELOG.md`
- `KNOWN_ISSUES.md`
- `RELEASE_CHECKLIST.md`
- `docs/release/v0.2.0-release-notes-draft.md`
- `docs/release/t9-1-release-docs-report.md`
- `docs/release/t9-1-final-release-readiness.md`
- `docs/release/t9-1-final-llm-smoke-recheck.md`
- `docs/archives/t8-writing-quality-closure.md`
- `docs/roadmaps/t9-stage-plan.md`

Expected absence:

- `docs/release/v0.2.0-release-report.md` is not in the tag, because it was created as post-release documentation on main after the tag.

## Tag Smoke Tests

### Backend Smoke

Command:

```powershell
python -m pytest backend/tests/test_pipeline.py backend/tests/test_beat_validator.py -q --tb=short
```

Result:

```text
75 passed in 8.68s
```

### Frontend Build

Command:

```powershell
cd frontend
npm run build
```

Result:

```text
passed
```

### Focused E2E

Command:

```powershell
npm run test:e2e:mock -- tests/e2e/14-candidate-workflow.spec.ts --reporter=line
```

Result:

```text
16 passed
```

## Return To Main

Commands:

```powershell
git checkout main
git pull --ff-only
git status --short
git log -1 --oneline
```

Result:

```text
main is up to date with origin/main
600969f docs: add v0.2.0 release report
working tree clean before this report was added
```

## Issues Found

None.

## Final Recommendation

The release is reproducible from the published `v0.2.0` tag. No tag / release inconsistency was found.

Recommended next step:

```text
Continue T9.2 planning and keep v0.2.0 as the current developer preview baseline.
```

