# v0.2.0 Release Report

Date: 2026-06-16

Release target:

```text
v0.2.0 - Writing Quality Loop Developer Preview
```

## Base Commit

Release-ready commit:

```text
6ef7d7893e22a93141ac5c23eb2053bd1d7877b4
docs: add T9.1 LLM smoke recheck
```

The `v0.2.0` tag was created before this release report commit, so the release artifact points at the release-ready source state rather than this post-release documentation update.

## Tag

Tag:

```text
v0.2.0
```

Tag type:

```text
annotated
```

Tag message:

```text
v0.2.0 Writing Quality Loop Developer Preview
```

Tag object:

```text
0ec1bbf304753a09c97f7c497240b1bad13d0f72
```

Peeled tag commit:

```text
6ef7d7893e22a93141ac5c23eb2053bd1d7877b4
```

Remote tag confirmation:

```text
0ec1bbf304753a09c97f7c497240b1bad13d0f72 refs/tags/v0.2.0
6ef7d7893e22a93141ac5c23eb2053bd1d7877b4 refs/tags/v0.2.0^{}
```

## GitHub Release

GitHub Release was created successfully as a prerelease.

Release URL:

[https://github.com/wuyinglai/moyun-studio/releases/tag/v0.2.0](https://github.com/wuyinglai/moyun-studio/releases/tag/v0.2.0)

Release title:

```text
v0.2.0 Writing Quality Loop Developer Preview
```

Prerelease:

```text
true
```

Published at:

```text
2026-06-16T03:43:24Z
```

Release notes source:

```text
docs/release/v0.2.0-release-notes-draft.md
```

## Preflight References

Release readiness was based on these reports:

- `docs/release/t9-1-release-docs-report.md`
- `docs/release/t9-1-final-release-readiness.md`
- `docs/release/t9-1-final-llm-smoke-recheck.md`
- `docs/archives/t8-writing-quality-closure.md`
- `docs/roadmaps/t9-stage-plan.md`

Key preflight results:

- Backend focused tests: passed.
- Frontend build: passed.
- Focused candidate E2E: passed.
- Full mock E2E: passed.
- Real LLM smoke: passed after T9.1-final-b endpoint recovery.
- Candidate safety smoke: passed.
- API key check: no real key committed.
- Working tree before tag: clean.

## Known Issues Summary

Known issues are tracked in:

```text
KNOWN_ISSUES.md
```

Release-relevant known issues:

- This is a developer preview, not a stable production release.
- Required beat validation is advisory and can return `warning` or `unknown`.
- Warning confirmation does not block adoption.
- Real LLM latency depends on the selected model service.
- Candidate/file write robustness still needs future atomic-write hardening.

## Final Status

```text
v0.2.0 developer preview released.
```

Release status:

- Tag created: yes.
- Tag pushed: yes.
- Remote tag confirmed: yes.
- GitHub prerelease created: yes.
- Product code changed during release operation: no.
- Prompt changed during release operation: no.
- `.env` changed: no.
- API key committed: no.

## Next Step

Recommended next step:

```text
Run a post-release checkout smoke against tag v0.2.0, then continue T9.2 planning.
```

