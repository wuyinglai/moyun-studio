# v0.2.1 Release Candidate Notes

## 基本信息

| 字段 | 值 |
|------|-----|
| Release | v0.2.1 RC |
| Base Commit | 8670ce1 |
| Date | 2026-06-17 |

---

## 版本定位

v0.2.1 是 v0.2.0 之后的维护增强版，主要包含写作质量增强能力：

- Quality Metadata MVP
- Repair Candidate MVP
- Continuity Anchors metadata 修复
- Real LLM dogfood 验证
- Repair safety boundary verification
- Pipeline prompt rendering contract cleanup

---

## Highlights

### Quality Metadata MVP

- 5 个质量维度自动计算：`instruction_following`, `continuity`, `style_preservation`, `change_scope`, `forbidden_check`
- `generate_quality_metadata()` 服务方法
- `CandidatePanel` quality summary badges 展示
- 与 beat_validation / continuity_anchors 集成

### Repair Candidate MVP

- `CandidateAction.REPAIR` 枚举值
- `create_repair_candidate()` 服务方法
- `POST /{project_id}/{candidate_id}/repair` API
- Repair prompt 模板（`prompts/pipeline/candidate-feedback/repair.md`）
- `hasRepairableWarning()` UI 按钮条件
- Repair child lineage（parent_candidate_id, revision_group_id, revision_index）

### Continuity Anchors Metadata 修复

- `ContinuityAnchorService.list_active()` 自动获取
- `ContinuityAnchorService.metadata()` 计算 used_count/anchor_ids/types
- `create_candidate()` 自动调用 service 获取 anchors
- `quality.continuity = PASS` 当 `used_count > 0`

### Safety Boundaries

- adopted/discarded parent 不可 revision（PARENT_NOT_PENDING → 409）
- adopted/discarded parent 不可 repair（PARENT_NOT_PENDING → 409）
- candidate-only 工作流，source adopt 前不变
- repair/revision 不修改 parent candidate
- 无自动 adopt，无自动覆盖正文

---

## Test Results

### Backend Tests

| 测试文件 | 结果 |
|---------|------|
| `test_pipeline.py` | 63 passed |
| `test_beat_validator.py` | 11 passed |
| `test_repair_candidate.py` | 9 passed |
| `test_candidate_quality_metadata.py` | 14 passed |
| `test_candidate_feedback_revision.py` | 10 passed |
| `test_continuity_anchors.py` | 8 passed |
| **总计** | **115 passed / 0 failed** |

### Frontend Build

**结果**：✅ built in 4.34s

### Focused E2E

| 测试文件 | 结果 |
|---------|------|
| `14-candidate-workflow.spec.ts` | 23 passed |
| `32-continuity-anchors.spec.ts` | 2 passed |
| **总计** | **25 passed / 0 failed** |

### Full Mock E2E

**结果**：77 passed / 93 skipped / 0 failed

### Secret Check

| 检查项 | 结果 |
|-------|------|
| 真实 API key 入库 | ✗ 无 |
| `.env` 在 gitignore | ✓ 是 |
| 报告包含真实 key | ✗ 无 |

---

## Real LLM Dogfood 摘要

### 执行情况

| Case | 类型 | 结果 |
|------|------|------|
| Case 1 | Rewrite + Continuity Anchors | ✅ |
| Case 2 | Polish Conservative | ✅ |
| Case 3 | Forbidden Reveal | ✅ |
| Case 4 | Relationship Jump | ✅ |
| Case 5 | Feedback Revision | ✅ |
| Case 6 | Repair Candidate | ✅ |
| Case 7 | Second Repair | ✅ |
| Case 8 | Continuity via Service | ✅ |

### 关键验证

- ✓ Repair coverage >= 2（Case 6 + Case 7）
- ✓ `continuity_anchors.used_count = 3`（Case 8）
- ✓ `quality.continuity = PASS`（Case 8）
- ✓ LLM 遵守 continuity anchors / forbidden beats
- ✓ Source adopt 前不变（所有 case）
- ✓ Parent candidate 不变（revision/repair）
- ✓ 无自动 adopt，无自动覆盖正文

---

## Known Issues

### 问题 1：Guardrails existing noise

**描述**：guardrails allowlist 存在噪音

**影响**：低，不影响核心功能

**建议**：后续专项清理

### 问题 2：缺少 T9.4a-b-c 实现文档

**描述**：T9.4a-b-c 的实现报告未单独创建

**影响**：低，功能已实现且测试通过

**建议**：后续技术文档整理时补充

---

## Upgrade Risks

| 风险 | 级别 | 说明 |
|------|------|-----|
| 数据兼容性 | 低 | 无数据库 schema 变化 |
| API 兼容性 | 低 | 新增 API，无破坏性变更 |
| UI 兼容性 | 低 | 新增 UI 元素，无破坏性变更 |
| Prompt 兼容性 | 低 | 新增 repair prompt，无破坏性变更 |

---

## Release Recommendation

**建议：可以进入 v0.2.1 正式 release**

理由：
1. T9.4 全部完成（Quality Metadata + Repair Candidate + Continuity Anchors）
2. T9.5 归档问题已不存在
3. 115 backend tests passed / 0 failed
4. frontend build passed
5. 77 full mock E2E passed / 0 failed
6. API key 安全检查通过
7. Real LLM dogfood 验证通过
8. Safety boundaries 验证通过

---

## Next Step

建议进入 **v0.2.1 正式 release**：
1. 创建 release branch（可选）
2. 打 v0.2.1 tag
3. 创建 GitHub Release
4. 发布 release notes