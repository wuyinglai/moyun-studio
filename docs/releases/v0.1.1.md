# Moyun Studio v0.1.1 Release Notes

## Release Type

Maintenance Release — documentation, onboarding, and release process improvements.

## Release Date

2026-05-22

## Summary

v0.1.1 is a documentation-focused maintenance release. No new features or code changes. The focus is on improving the first-time user experience: clearer installation instructions, better configuration guidance, known issues documentation, and a formalized release checklist.

## What's Improved

### README.md — Enhanced with project structure and navigation

- Added **version badge** (v0.1.1) with link to release notes and changelog
- Added **Project Structure** section showing directory layout
- Added **Roadmap** link to `docs/roadmap.md`
- Added reference to `examples/basic-novel-project/` minimal example
- Updated documentation links to point to `docs/known-issues.md` and `docs/changelog.md`
- Retained Quick Start, Available Commands, and Configuration sections

### docs/quick-start.md — New

- Step-by-step setup walkthrough from clone to first scene
- DeepSeek and Ollama configuration examples
- Expected output for each step
- Troubleshooting table for common startup issues
- Links to Known Issues, Roadmap, and Changelog

### docs/known-issues.md — New

- 4 current limitations with impact assessment and fix plans
- 6 common startup failure scenarios with step-by-step fixes
- Workarounds table for quick reference
- Future fix directions for v0.2

### docs/roadmap.md — New

- Planned features organized by version milestones (v0.1.x through v0.4)
- Long-term vision statement
- Link to GitHub issues for feature requests

### docs/changelog.md — New

- Changelog following [Keep a Changelog](https://keepachangelog.com/) format
- v0.1.0 record with all safety packages (A-E)
- v0.1.1 record with documentation improvements

### examples/basic-novel-project/ — New

- Minimal example project: "山城雾事" (urban suspense)
- Includes `meta.json`, `outline.md`, `ch-meta.json`, and one scene file
- Demonstrates standard project structure for quick reference

### scripts/verify-release.ps1 — New

- Verification script for release readiness
- Checks: git status, backend dependencies, frontend install/build/lint, documentation files, example project

## Startup / Configuration Notes

- No code changes in this release. Backend and frontend behavior is identical to v0.1.0.
- The `.env.example` file now documents the `MOYUN_DISABLE_PROXY_DETECTION` environment variable, which resolves SSL connection errors on Windows when proxy settings interfere with aiohttp/httpx.
- If you are upgrading from v0.1.0, no `.env` changes are required unless you are experiencing Windows SSL issues.

## Known Issues

Unchanged from v0.1.0. See [docs/known-issues.md](../known-issues.md) for the full list:

1. No conflict detection on memory/material endpoints
2. Some non-core API files still use raw I/O
3. Real LLM E2E is optional (not in default release check)
4. Workspace `.config.json` stores API keys locally (intentional for local-first)

## Upgrade Notes

- No breaking changes
- No dependency changes
- No migration required
- Optional: review the new `.env.example` for additional configuration options

## Verification

| Check | Result |
|-------|--------|
| `npm install` (frontend) | Passed |
| `npm run build` (frontend) | Passed — 0 errors |
| `npm run dev` (frontend) | Passed — starts on port 5173 |
| `npm run lint` (frontend) | Passed — 0 errors, warnings (pre-existing, non-blocking) |
| `docs/quick-start.md` exists | Yes |
| `docs/known-issues.md` exists | Yes |
| `docs/roadmap.md` exists | Yes |
| `docs/changelog.md` exists | Yes |
| `examples/basic-novel-project/` exists | Yes |
| No code changes | Confirmed — documentation-only release |
