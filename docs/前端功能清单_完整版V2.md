# 墨韵 - 前端功能清单（完整版 V2）

> **说明**：
> - 默认所有功能都保留，不标记即保留
> - 如需删除，在「删除」列打 `✓`
> - 如需调整功能，在「调整/备注」列填写

---

## 一、顶部工具栏 (AppHeader.vue)

### 1.1 Logo 区域
| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| H001 | Logo显示 | **纯展示**<br>无数据来源，无操作行为<br>显示"墨韵"文字 + 内联 SVG 图标 | ☐ | |

### 1.2 项目信息
| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| H002 | 项目名称显示 | **读取**<br>`projectStore.currentProject` → `.name`<br>显示项目名称 | ☐ | |
| H003 | 项目名称编辑 | **点击 → 变成输入框**<br>`isEditingName.value = true`<br><br>**输入完成(Enter/失焦) → 验证**<br>检查 `name.value.trim()` 非空<br><br>**→ 调用 Store**<br>`projectStore.updateProject(projectStore.currentProject.id, { name: newName })`<br><br>**→ 调用后端 API**<br>`PUT /api/projects/{id}`<br>请求体: `{ id, name, author, genre, ... }`<br>响应: `{ success, data: Project }`<br><br>**→ Store 更新**<br>`projectStore.currentProject.name = newName`<br>`projectStore.projects` 中对应项同步更新<br><br>**→ 通知**<br>成功: `notification.success('项目名已更新')`<br>失败: `notification.error('更新项目名失败')` | ☐ | |

### 1.3 LLM 状态
| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| H004 | 连接状态指示 | **读取**<br>`llmStore.isConnected` → 布尔值<br><br>**显示**<br>`true` → 绿色圆点<br>`false` → 红色圆点<br><br>**点击 → 打开设置**<br>`uiStore.openSettings()` | ☐ | |
| H005 | SSE状态指示 | **读取**<br>`sseService.isConnected` → 布尔值<br><br>**显示**<br>已连接 → 绿色文字标签 `SSE`<br>未连接 → 灰色文字标签 `SSE`<br>位于 LLM 状态圆点旁 | ☐ | |
| H006 | LLM生成动画 | **读取**<br>`llmStore.isGenerating` → 布尔值<br>`chatStore.generationMode` → 'continue' \| 'rewrite' \| 'expand' \| 'polish'<br><br>**显示**<br>生成中: 旋转图标 + 动态文字<br>`generationMode === 'continue'` → "正在续写章节..."<br>`generationMode === 'rewrite'` → "正在重写章节..."<br>其他 → "AI生成中..." | ☐ | |
| H007 | Thinking开关 | **点击 → 取反**<br>`llmStore.config.thinking = !llmStore.config.thinking`<br><br>**→ 调用 Store**<br>`llmStore.saveConfig({ thinking: newValue })`<br><br>**→ 调用后端 API**<br>`POST /api/llm/config`<br>请求体: `{ apiType, apiUrl, apiKey, model, thinking }`<br>响应: `{ success, data: LLMConfig }`<br><br>**→ Store 更新**<br>`llmStore.config.thinking` | ☐ | |

### 1.4 操作按钮
| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| H008 | 打开项目按钮 | **点击**<br>`uiStore.openOpenProject()`<br><br>**→ Store 更新**<br>`uiStore.modals.openProject = true`<br>其他 modals 设为 false<br><br>**→ OpenProjectModal 打开 (watch)**<br>自动触发 `projectStore.loadProjects()` | ☐ | |
| H009 | 新建项目按钮 | **点击**<br>`uiStore.openCreateProject()`<br><br>**→ Store 更新**<br>`uiStore.modals.createProject = true`<br>`uiStore.modals.editingProject = null`<br>其他 modals 设为 false | ☐ | |
| H010 | 设置按钮 | **点击**<br>`uiStore.openSettings()`<br><br>**→ Store 更新**<br>`uiStore.modals.settings = true`<br>其他 modals 设为 false | ☐ | |

---

## 二、左侧文件树 (FileTree.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| F001 | 标题栏"文件" | **纯展示**<br>无数据来源，无操作行为 | ☐ | |
| F002 | 刷新按钮 | **点击**<br>`fileStore.loadTree(projectStore.currentProject.id)`<br><br>**→ Store 更新**<br>`fileStore.isLoading = true`<br><br>**→ 调用后端 API**<br>`GET /api/tree?project_id={projectId}`<br>响应: `{ tree: FileNode[] }`<br><br>**→ Store 更新**<br>`fileStore.tree = response.tree`<br>`fileStore.isLoading = false`<br><br>**→ 通知**<br>`notification.success('文件树已刷新')` | ☐ | |
| F003 | 空状态提示 | **条件渲染**<br>`projectStore.currentProject === null`<br>→ 显示: "请先打开项目"<br>→ 否则: 渲染文件树 | ☐ | |
| F004 | 加载状态 | **条件渲染**<br>`fileStore.isLoading === true`<br>→ 显示: 旋转图标 + "加载中..." | ☐ | |
| F005 | 文件夹显示 | **渲染**<br>`fileStore.tree` → 递归渲染文件夹和文件<br><br>**初始加载**<br>组件挂载时自动调用 `fileStore.loadTree()` | ☐ | |
| F006 | 文件夹展开/折叠 | **点击文件夹**<br>切换 `isExpanded` 状态<br>仅本地 UI 状态，无后端交互 | ☐ | |
| F007 | 文件点击打开 | **点击文件节点**<br>`fileStore.readFile(projectId, node.path)`<br><br>**→ Store 更新**<br>`fileStore.isLoading = true`<br><br>**→ 调用后端 API**<br>`GET /api/file?project_id={projectId}&path={path}`<br>响应: `{ content, frontmatter: { wordCount }, wordCount }`<br><br>**→ Store 更新**<br>`fileStore.openFile(node)` → 添加到 `openFiles`<br>`fileStore.isLoading = false`<br><br>**→ 编辑器加载**<br>`editorStore.loadContent(path, content)` → 更新 `contents[path]`<br>`editorStore.setCurrentFile(path)` → 更新 `currentFilePath`<br><br>**→ App.vue watch 触发 (G007)**<br>`watch(currentFilePath, (path) => {`<br>`  const prompt = editorStore.getFilePrompt(path)`<br>`  rightPanelStore.updatePrompt(prompt)`<br>`})`<br><br>**→ 通知**<br>`notification.error('无法打开文件')` | ☐ | |

---

## 三、中间编辑区

### 3.1 编辑器标签页 (EditorTabs.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| T001 | 标签栏显示 | **渲染**<br>`fileStore.openFiles.map(file => ({ path, name, icon }))` | ☐ | |
| T002 | 文件图标 | **读取**<br>根据 `file.name` 扩展名判断<br>`md/txt/markdown` → 文件行图标<br>`json/yaml/yml` → 代码图标 | ☐ | |
| T003 | 文件名显示 | **读取**<br>`file.name` | ☐ | |
| T004 | 脏标记 | **读取**<br>`fileStore.unsavedFiles.has(file.path)`<br>→ `true` → 显示红色圆点动画<br>→ `false` → 不显示 | ☐ | |
| T005 | 标签点击切换 | **点击标签**<br>`fileStore.openFiles.find(f => f.path === clickedPath)`<br><br>**→ Store 更新**<br>`fileStore.currentFile = file`<br>`editorStore.setCurrentFile(file.path)`<br><br>**→ MarkdownEditor watch 触发**<br>`watch(currentFilePath, (newPath) => {`<br>`  const content = editorStore.contents[newPath]`<br>`  updateEditorContent(content)`<br>`})` | ☐ | |
| T006 | 关闭按钮 | **点击 (stopPropagation)**<br><br>**→ 检查脏文件**<br>`if (fileStore.unsavedFiles.has(path)) {`<br>`  const confirmed = window.confirm('文件有未保存的更改，确定要关闭吗？')`<br>`  if (!confirmed) return`<br>`}`<br><br>**→ Store 更新**<br>`fileStore.closeFile(path)` → 从 `openFiles` 移除<br>`editorStore.clearFile(path)` → 从 `contents` 移除 | ☐ | |
| T007 | 字数统计 | **读取**<br>`editorStore.wordCount` → 数字<br>格式化为 `xxx,xxx 字` | ☐ | |
| T008 | 光标位置 | **读取**<br>`editorStore.cursorPosition` → `{ line: number, col: number }`<br>显示 `Ln X, Col Y` | ☐ | |
| T009 | 保存状态 | **读取**<br>`fileStore.isSaving` → 布尔值<br>`fileStore.unsavedFiles.has(path)` → 布尔值<br><br>**显示**<br>`isSaving === true` → "保存中..."<br>`unsavedFiles.has(path) === true` → "未保存" (黄色)<br>`其他` → "已保存" (绿色) | ☐ | |

### 3.2 编辑器工具栏 (EditorToolbar.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| TB001 | 后退按钮 | **点击 (检查 `historyStore.canGoBack()`)**<br>`historyStore.goBack(path)`<br><br>**→ 获取历史内容**<br>`const prevContent = historyStore.goBack(currentFilePath)`<br><br>**→ Store 更新**<br>`editorStore.setContent(prevContent)` → `contentSource = 'external'`<br><br>**→ MarkdownEditor watch 触发**<br>`watch(contents[path] + contentSource === 'external', () => {`<br>`  replaceEditorContent(prevContent)`<br>`})` | ☐ | |
| TB002 | 前进按钮 | **点击 (检查 `historyStore.canGoForward()`)**<br>`historyStore.goForward(path)`<br><br>**→ 获取历史内容**<br>`const nextContent = historyStore.goForward(currentFilePath)`<br><br>**→ Store 更新**<br>`editorStore.setContent(nextContent)` → `contentSource = 'external'` | ☐ | |
| TB003 | 预览/编辑切换 | **点击**<br>`isPreviewMode.value = !isPreviewMode.value`<br><br>**→ 条件渲染**<br>`true` → 显示 `<div v-html="previewHtml">` (Markdown 转 HTML)<br>`false` → 显示 CodeMirror 编辑器 | ☐ | |
| TB004 | 停止按钮 | **条件显示 (任一为 true)**<br>`chatStore.isStreaming`<br>`fileGen.isGenerating`<br>`taskQueue.isProcessing`<br>`guide.isRunning`<br><br>**点击**<br>`chatStore.cancelStream()` → `streamController.abort()`<br>`fileGen.cancelGeneration()` → `_abortController.abort()`<br>`cancelQueuedTask(runningTaskId)` → `taskStore.cancelTask(id)`<br><br>**→ 调用后端 API (任务取消)**<br>`POST /api/tasks/{taskId}/cancel`<br><br>**→ Store 更新**<br>`chatStore.isStreaming = false`<br>`fileGen.isGenerating = false`<br>`taskStore.tasks[id].status = 'cancelled'` | ☐ | |
| TB005 | 润色按钮 | **点击**<br>`validateProjectAndFile()` → 检查 `projectStore.currentProject` 和 `editorStore.currentFilePath`<br><br>**→ 调用管线**<br>`taskQueue.enqueue(() => fileGen.runPipeline('polish'))`<br><br>**→ Store 更新**<br>`taskStore.addTask(taskId, 'polish')`<br>`taskStore.startTask(taskId)`<br><br>**→ 调用后端 API**<br>`POST /api/pipeline/run`<br>请求体: `{ pipeline: 'polish', project_id, target_file, output_mode: 'overwrite', context }`<br><br>**→ SSE 监听**<br>`sseService.on('generation', (data) => { ... })` → 实时更新编辑器 | ☐ | |
| TB006 | 生成按钮 | 同 TB005，但 `pipeline: 'generate'`、`output_mode: 'append'` | ☐ | |
| TB007 | 重写按钮 | 同 TB005，但 `pipeline: 'rewrite'`、`output_mode: 'overwrite'` | ☐ | |
| TB008 | 提取按钮 | 同 TB005，但 `pipeline: 'extract'` | ☐ | |
| TB009 | 自定义管线下拉 | **点击展开**<br>`pipelineStore.pipelines.filter(p => p.source === 'custom')`<br><br>**选择管线**<br>调用 `runPipeline(selectedPipelineKey)`<br>同 TB005 流程 | ☐ | |
| TB010 | 写下一部分 | **点击**<br>`const newPath = incrementFileName(currentFilePath)`<br><br>**→ 调用后端 API**<br>`POST /api/file/create`<br>请求体: `{ project_id, path: newPath, content: '' }`<br><br>**→ Store 更新**<br>`fileStore.createFile(newPath, node)`<br><br>**→ 打开新文件**<br>`fileStore.openFile(node)`<br>`editorStore.setCurrentFile(newPath)`<br><br>**→ 触发生成**<br>`fileGen.runPipeline('generate')`<br><br>**→ SSE 监听**<br>`sseService.on('generation', (data) => {`<br>`  appendEditorContent(data.content)`<br>`})` | ☐ | |
| TB011 | 更多菜单 | **点击**<br>`moreMenuVisible.value = !moreMenuVisible.value`<br>显示 Dropdown 菜单 | ☐ | |
| TB012 | - Token统计 | **点击**<br>`uiStore.openTokenCount()` → `modals.tokenCount = true` | ☐ | |
| TB013 | - 对比 | **点击**<br>`uiStore.openCompare()` → `modals.compare = true` | ☐ | |
| TB014 | - 反馈 | **点击**<br>`uiStore.openFeedback()` → `modals.feedback = true` | ☐ | |
| TB015 | - 修改日志 | **点击**<br>`uiStore.openRevisionLog()` → `modals.revisionLog = true` | ☐ | |
| TB016 | - 批量生成 | **点击**<br>`uiStore.openBatchGenerate()` → `modals.batchGenerate = true` | ☐ | |
| TB017 | - 质量审查 | **点击**<br>`uiStore.openQualityReview()` → `modals.qualityReview = true` | ☐ | |

### 3.3 Markdown编辑器 (MarkdownEditor.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| E001 | 欢迎页面 | **条件渲染**<br>`fileStore.currentFile === null`<br>→ 显示欢迎卡片<br>→ "开始创作" → `uiStore.openCreateProject()`<br>→ "打开项目" → `uiStore.openOpenProject()` | ☐ | |
| E002 | CodeMirror编辑器 | **初始化 (onMounted)**<br>创建 `EditorView` 实例，配置扩展:<br>`lineNumbers()` → 行号<br>`highlightActiveLine()` → 高亮当前行<br>`drawSelection()` → 选择绘制<br>`history()` → 历史记录<br>`markdown()` → Markdown 语法<br>`syntaxHighlighting(defaultHighlightStyle)` → 语法高亮<br>`semanticHighlight()` → 语义着色扩展<br>`keymap.of([...])` → 快捷键绑定 | ☐ | |
| E003 | 内容变化监听 | **CodeMirror updateListener**<br>`docChanged: true` 时触发<br><br>**→ 调用 Store**<br>`editorStore.updateContent(path, newContent)`<br>`fileStore.markDirty(path)` → `unsavedFiles.add(path)`<br>`editorStore.markLocalEdit()` → `contentSource = 'local'`<br><br>**→ 触发自动保存**<br>`debouncedAutoSave(path)` 500ms 后执行 | ☐ | |
| E004 | 自动保存 | **useAutoSave composable**<br><br>**触发 (debounce 500ms)**<br>`fileStore.saveFile(projectId, path, content)`<br><br>**→ 调用后端 API**<br>`POST /api/file`<br>请求体: `{ path, content }`<br><br>**→ Store 更新**<br>`fileStore.unsavedFiles.delete(path)`<br>`fileStore.isSaving = false` | ☐ | |
| E005 | 语义着色 | **CodeMirror ExtensionFactory**<br>正则匹配:<br>`「([^」]+)」` → 对话 → 绿色 `#7ecf8a`<br>`《([^》]+)》` → 作品名 → 蓝色<br>`^第[一二三四五六七八九十百千零〇\\d]+[章节卷集部]\\s*「([^」]+)」` → 章节标题 → 黄色<br>`^(?!.*[「《]).{8,20}$` (短行) → 可能是人物 → 蓝色<br>`^[*\\-+•·]\\s` → 列表项 → 灰色 | ☐ | |
| E006 | 行号显示 | **CodeMirror Extension**<br>`lineNumbers()` | ☐ | |
| E007 | 光标跟踪 | **CodeMirror Extension**<br>`updateListener` 中 `selectionSet` 时触发<br><br>**→ 计算位置**<br>`const pos = view.state.selection.main.head`<br>`const line = view.state.doc.lineAt(pos)`<br><br>**→ Store 更新**<br>`editorStore.cursorPosition = { line: line.number, col: pos - line.from + 1 }` | ☐ | |
| E008 | 外部内容更新 | **watch**<br>`watch([() => editorStore.contents[currentFilePath], () => editorStore.contentSource], ([content, source]) => {`<br>`  if (source === 'external' && content !== undefined) {`<br>`    if (content !== view.state.doc.toString()) {`<br>`      view.dispatch({`<br>`        changes: { from: 0, to: view.state.doc.length, insert: content }`<br>`      })`<br>`    }`<br>`  }`<br>`})` | ☐ | |
| E009 | 预览渲染 | **useMarkdownPreview composable**<br><br>**触发 (isPreviewMode 变化)**<br>`marked.parse(markdownContent)` → HTML<br><br>**→ 更新**<br>`previewHtml.value = html` | ☐ | |
| E010 | 行跳转 | **window 事件监听**<br>`window.addEventListener('editor:jump-to-line', (e) => {`<br>`  const { line } = e.detail`<br>`  const lineObj = view.state.doc.line(line)`<br>`  view.dispatch({`<br>`    selection: { anchor: lineObj.from }`,<br>`    scrollIntoView: true`<br>`  })`<br>`})` | ☐ | |

---

## 四、聊天面板 (ChatPanel.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| C001 | 消息列表 | **渲染**<br>`chatStore.messages.map(msg => ({ role, content, timestamp }))` | ☐ | |
| C002 | 思考过程 | **读取**<br>`chatStore.currentThinking` → 字符串<br><br>**显示区域 (非空时)**<br>`v-if="currentThinking"` | ☐ | |
| C003 | 用户消息气泡 | **条件渲染**<br>`msg.role === 'user'`<br>右侧对齐，蓝色背景 | ☐ | |
| C004 | AI消息气泡 | **条件渲染**<br>`msg.role === 'ai'`<br>左侧对齐，白色背景 | ☐ | |
| C005 | 消息时间戳 | **读取**<br>`msg.timestamp` → Unix 时间戳<br>格式化为 `HH:mm` | ☐ | |
| C006 | 加载动画 | **条件渲染**<br>`chatStore.isStreaming === true`<br>→ 显示旋转图标 + "正在思考..." | ☐ | |
| C007 | 聊天输入框 | **双向绑定**<br>`chatInput.value = inputText`<br>自动调整高度 (max 200px) | ☐ | |
| C008 | 发送消息 | **点击发送 / 按 Enter**<br><br>**→ 验证**<br>`if (!llmStore.isConnected) {`<br>`  notification.warning('请先连接 LLM')`<br>`  return`<br>`}`<br><br>**→ 清空输入**<br>`inputText.value = ''`<br><br>**→ Store 更新**<br>`chatStore.addUserMessage(text)` → 添加到 `messages`<br><br>**→ 调用后端 API (SSE)**<br>`fetch('/api/chat', {`<br>`  method: 'POST',`<br>`  body: JSON.stringify({ message, project_id, context_file })),`<br>`  headers: { 'Content-Type': 'text/event-stream' }`<br>`})`<br><br>**→ Store 更新 (SSE 流)**<br>`chatStore.appendAIMessage(delta)`<br><br>**→ 完成**<br>`chatStore.finishAIMessage()` | ☐ | |
| C009 | 取消生成 | **条件显示**<br>`chatStore.isStreaming === true`<br><br>**点击**<br>`chatStore.cancelStream()`<br>`streamController.abort()`<br><br>**→ Store 更新**<br>`chatStore.isStreaming = false`<br>`chatStore.finishAIMessage()` | ☐ | |
| C010 | 自动滚动 | **watch**<br>`watch(() => chatStore.messages.length, async () => {`<br>`  await nextTick()`<br>`  messagesContainer.value.scrollTop = messagesContainer.value.scrollHeight`<br>`})` | ☐ | |

---

## 五、右侧面板 (RightPanel.vue)

### 5.1 Tab 切换

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| P001 | 快捷Tab | **点击**<br>`activeTab.value = 'prompt'`<br>→ 显示 PromptPanel | ☐ | |
| P002 | 管线编辑Tab | **点击**<br>`activeTab.value = 'pipeline'`<br>→ 显示 PipelineEditor | ☐ | |
| P003 | 工作流Tab | **点击**<br>`activeTab.value = 'workflow'`<br>→ 显示 WorkflowPanel | ☐ | |
| P004 | 故事状态Tab | **点击**<br>`activeTab.value = 'story'`<br>→ 显示 StoryStatePanel | ☐ | |
| P005 | 文风Tab | **点击**<br>`activeTab.value = 'style'`<br>→ 显示 StyleGuidePanel | ☐ | |
| P006 | 执行Tab | **点击**<br>`activeTab.value = 'execution'`<br>→ 显示 ExecutionPanel | ☐ | |

### 5.2 Prompt面板 (PromptPanel.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| PP001 | 创作任务标题 | **读取**<br>`guide.workflowLabel` → 字符串 | ☐ | |
| PP002 | 刷新工作流 | **点击**<br>`guide.reset()` → 重置所有步骤为 pending<br>`guide.loadWorkflow()`<br><br>**→ 调用后端 API**<br>`GET /api/workflows/full-novel`<br>响应: `{ workflow: { name, label, steps: [...] } }`<br><br>**→ 更新**<br>`_workflowName`, `_workflowLabel`, `_steps` | ☐ | |
| PP003 | 步骤列表 | **渲染**<br>`guide.steps.map(step => ({ icon, label, status }))`<br><br>**状态图标**<br>`pending` → □<br>`running` → ◌<br>`done` → ✅<br>`paused` → ⏸ | ☐ | |
| PP004 | 步骤执行按钮 | **条件显示**<br>`step.status === 'pending' && !guide.isRunning`<br><br>**点击**<br>`guide.reset()` → 重置所有步骤<br>`guide.currentStepIndex = index`<br>`guide.start(projectId, filePath)`<br><br>**→ 执行单个步骤**<br>调用 `runFromCurrent()` 从指定步骤开始 | ☐ | |
| PP005 | 开始创作按钮 | **点击**<br>`guide.start(projectId, filePath)`<br><br>**→ Store 更新**<br>`_isRunning = true`<br>`_shouldStop = false`<br>`currentStepIndex = 0`<br><br>**→ 执行工作流**<br>`runFromCurrent()` 循环执行步骤<br>每步调用 `fileGen.runPipeline(step.pipeline)`<br><br>**→ L1 模式 (需确认)**<br>每步完成后设置 `isPaused = true`<br>用户需点击"继续"或"停止" | ☐ | |
| PP006 | 停止按钮 | **条件显示**<br>`guide.isRunning === true`<br><br>**点击**<br>`guide.stopAfterCurrent()`<br><br>**→ Store 更新**<br>`_shouldStop = true`<br><br>**→ 不再继续**<br>当前步骤完成后 `runFromCurrent()` 不再调用下一步 | ☐ | |
| PP007 | 重置按钮 | **条件显示**<br>`steps.some(s => s.status === 'done')`<br><br>**点击**<br>`guide.reset()`<br><br>**→ Store 更新**<br>所有步骤 `status = 'pending'`<br>`_isRunning = false`<br>`_shouldStop = false` | ☐ | |
| PP008 | 进度条 | **读取**<br>`guide.progress` → `{ done: number, total: number, percent: number }`<br>显示 `done/total` 和进度条 | ☐ | |
| PP009 | 暂停提示 | **条件显示**<br>`guide.isPaused === true`<br><br>**→ 显示**<br>`steps.some(s => s.status === 'waiting')` → "已完成！点击顶部工具栏「写下一部分」继续"<br>`其他` → "已停止，可点击顶部工具栏「写下一部分」恢复" | ☐ | |
| PP010 | 生成状态提示 | **条件显示**<br>`fileGen.isGenerating === true`<br>显示 "AI正在生成..." | ☐ | |
| PP011 | 后退按钮 | **点击**<br>`rightPanelStore.goPromptHistoryBack()`<br><br>**→ Store 更新**<br>`currentHistoryIndex++`<br>`promptContent = promptHistory[currentHistoryIndex]` | ☐ | |
| PP012 | 前进按钮 | **点击**<br>`rightPanelStore.goPromptHistoryForward()`<br><br>**→ Store 更新**<br>`currentHistoryIndex--`<br>`promptContent = promptHistory[currentHistoryIndex]` | ☐ | |
| PP013 | 清空历史 | **点击**<br>`rightPanelStore.clearHistory()`<br><br>**→ Store 更新**<br>`promptHistory = []`<br>`currentHistoryIndex = -1` | ☐ | |
| PP014 | 版本历史展开 | **点击**<br>`showArchive.value = !showArchive.value`<br><br>**→ 调用后端 API**<br>`GET /api/prompts/version-all`<br>响应: `{ versions: [{ name, type, created_at }] }`<br><br>**→ 过滤显示**<br>`archiveVersions = versions.filter(v => v.type === 'file' \|\| v.type === 'directory')` | ☐ | |
| PP015 | 版本内容预览 | **点击版本项**<br>`if (selectedVersion === name) {`<br>`  selectedVersion = null`<br>`} else {`<br>`  selectedVersion = name`<br>`}`<br><br>**→ 调用后端 API**<br>`GET /api/prompts/version-content/{name}`<br>响应: `{ content }`<br><br>**→ 更新**<br>`archiveContent = content` | ☐ | |
| PP016 | 版本恢复 | **点击"恢复此版本"**<br><br>**→ 调用后端 API**<br>`POST /api/prompts/version-restore/{name}`<br><br>**→ 通知**<br>`notification.success('已恢复到此版本')`<br><br>**→ 重新加载**<br>`loadVersionList()` | ☐ | |
| PP017 | Prompt编辑区 | **双向绑定**<br>`localPrompt.value = rightPanelStore.promptContent`<br><br>**输入触发**<br>`handlePromptInput()` | ☐ | |
| PP018 | Prompt自动保存 | **触发 (debounce 500ms)**<br>`rightPanelStore.updatePrompt(localPrompt)`<br><br>**→ Store 更新**<br>`promptContent = localPrompt` | ☐ | |
| PP019 | 引用文件显示 | **Computed**<br>正则 `/@\{([^}]+)\}/g` 提取 `@{path}`<br><br>**点击**<br>`fileStore.openFile(node)`<br>`editorStore.setCurrentFile(path)` | ☐ | |
| PP020 | 变量提示 | **静态展示**<br>显示 `{{变量名}}` 格式提示 | ☐ | |

### 5.3 执行面板 (ExecutionPanel.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| EP001 | 状态指示 | **读取**<br>`taskStore.tasks.some(t => t.status === 'running')`<br><br>**显示**<br>`true` → "运行中" + 绿色点<br>`false` → "空闲" + 灰色点 | ☐ | |
| EP002 | 任务卡片列表 | **渲染**<br>`taskStore.tasks.map(task => ({ id, name, status, progress }))` | ☐ | |
| EP003 | 任务状态徽章 | **读取**<br>`task.status`<br><br>**显示**<br>`'pending'` → 灰色 "等待中"<br>`'running'` → 蓝色 "进行中"<br>`'done'` → 绿色 "已完成"<br>`'failed'` → 红色 "失败"<br>`'cancelled'` → 灰色 "已取消"<br>`'waiting'` → 黄色 "待确认" | ☐ | |
| EP004 | 任务进度条 | **条件显示**<br>`task.status === 'running'`<br>根据 `task.progress` (0-100) 显示进度条 | ☐ | |
| EP005 | 继续按钮 | **条件显示**<br>`task.status === 'waiting'`<br><br>**点击**<br>`confirmTask(taskId)`<br><br>**→ Store 更新**<br>`taskStore.confirmTask(taskId)` → `status = 'done'` | ☐ | |
| EP006 | 取消按钮 | **条件显示**<br>`task.status === 'pending' \|\| task.status === 'running'`<br><br>**点击**<br>`cancelTask(taskId)`<br><br>**→ 调用后端 API**<br>`POST /api/tasks/{taskId}/cancel`<br><br>**→ Store 更新**<br>`taskStore.cancelTask(taskId)` → `status = 'cancelled'` | ☐ | |
| EP007 | 清空任务 | **点击**<br>`taskStore.clearTasks()`<br><br>**→ Store 更新**<br>`tasks = []`<br>`queue = []`<br><br>**→ 通知**<br>`notification.success('任务队列已清空')` | ☐ | |
| EP008 | AI修改摘要 | **读取**<br>`useDiffSummary().summary` → 字符串<br><br>**点击关闭**<br>`diffSummary.dismiss()` → `summary = null` | ☐ | |
| EP009 | 执行日志 | **渲染**<br>`taskStore.logs.map(log => ({ level, message, timestamp }))`<br><br>**自动滚动 (watch)**<br>`watch(logs.length, scrollToBottom())` | ☐ | |
| EP010 | 日志清空 | **点击**<br>`taskStore.clearLogs()`<br><br>**→ Store 更新**<br>`logs = []` | ☐ | |

---

## 六、模态框

### 6.1 新建项目 (CreateProjectModal.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| M001 | 项目名称输入 | **双向绑定**<br>`wizard.params.name`<br>必填项验证 | ☐ | |
| M002 | 题材选择 | **渲染选项**<br>`customParamsStore.getOptions('genre')`<br><br>**添加选项**<br>`customParamsStore.addOption('genre', newValue)` → 保存到 localStorage<br><br>**删除选项**<br>`customParamsStore.removeOption('genre', value)` | ☐ | |
| M003 | 写作风格 | 同 M002，key = 'writing_style' | ☐ | |
| M004 | 基调选择 | 同 M002，key = 'tone' | ☐ | |
| M005 | 背景选择 | 同 M002，key = 'background' | ☐ | |
| M006 | 主题选择 | 同 M002，key = 'theme' | ☐ | |
| M007 | 作品规模 | **预设选项**<br>5万字 (50000) / 10万字 (100000) / 15万字 (150000) / 20万字 (200000) | ☐ | |
| M008 | 作者输入 | **双向绑定**<br>`wizard.params.author` (可选) | ☐ | |
| M009 | 高级设置折叠 | **Ant Design Collapse**<br>`v-model:activeKey` 控制展开项 | ☐ | |
| M010 | 生成并打开 | **点击**<br><br>**→ 验证**<br>`if (!wizard.params.genre) {`<br>`  notification.warning('请选择题材')`<br>`  return`<br>`}`<br><br>**→ 调用后端 API**<br>`POST /api/projects`<br>请求体: `{ name, author, genre, tone, background, theme, writing_style, target_word_count }`<br>响应: `{ success, data: { project_id, ... } }`<br><br>**→ 创建初始文件**<br>`POST /api/file/create`<br>请求体: `{ project_id, path: '书名与创意.md', content: '' }`<br><br>**→ Store 更新**<br>`projectStore.pendingGeneration = { projectId, filePath, params }`<br><br>**→ 路由跳转**<br>`router.push('/project/{projectId}')`<br><br>**→ App.vue 监听 (G006)**<br>`watch(pendingGeneration, async () => {`<br>`  // 自动生成...`<br>`})` | ☐ | |
| M011 | 参数管理 | **点击笔按钮**<br>`showOptionsEditor.value = !showOptionsEditor.value`<br><br>**添加**<br>`customParamsStore.addOption(key, value)` → 保存到 localStorage<br><br>**删除**<br>`customParamsStore.removeOption(key, value)`<br><br>**→ 后端同步**<br>`customParamsStore.syncToServer()` → `POST /api/config` 同步到 `.config.json` | ☐ | |

### 6.2 打开项目 (OpenProjectModal.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| O001 | 项目列表加载 | **watch visible 变化**<br>`watch(() => modals.openProject, (visible) => {`<br>`  if (visible) projectStore.loadProjects()`<br>`})`<br><br>**→ 调用后端 API**<br>`GET /api/projects`<br>响应: `{ projects: [...], total }`<br><br>**→ Store 更新**<br>`projectStore.projects = response.projects`<br>`projectStore.total = response.total` | ☐ | |
| O002 | 项目卡片显示 | **渲染**<br>`projects.map(p => ({ id, name, genre, wordCount, createdAt, completion, author }))`<br><br>**封面生成**<br>基于 `genre` 生成渐变背景 | ☐ | |
| O003 | 项目选中 | **单击**<br>`selectedId.value = item.id` | ☐ | |
| O004 | 双击打开 | **dblclick**<br>`openProject(item.id)` | ☐ | |
| O005 | 删除项目 | **点击删除按钮**<br><br>**→ 显示确认 Popconfirm**<br><br>**确认后 → 调用后端 API**<br>`DELETE /api/projects/{id}`<br><br>**→ Store 更新**<br>`projects.remove(item)`<br>`if (selectedId === id) selectedId = null` | ☐ | |
| O006 | 打开按钮 | **点击 (检查 `selectedId`)**<br><br>**→ 调用后端 API**<br>`GET /api/projects/{id}`<br>响应: `{ project: ProjectDetail }`<br><br>**→ Store 更新**<br>`projectStore.currentProject = response.project`<br><br>**→ 加载文件树**<br>`fileStore.loadTree(projectId)`<br><br>**→ 通知**<br>`notification.success('已打开项目：xxx')`<br><br>**→ 关闭 Modal**<br>`uiStore.closeOpenProject()` | ☐ | |
| O007 | 空状态 | **条件渲染**<br>`projects.length === 0`<br>显示 Ant Design Empty 组件 | ☐ | |

### 6.3 设置 (SettingsModal.vue)

| 编号 | 功能 | 完整操作流程（UI → Store → API → Store更新） | 删除 | 调整/备注 |
|------|------|------------------------------------------------|------|-----------|
| S001 | AI设置Tab | **默认显示 Tab** | ☐ | |
| S002 | 自动化Tab | **Tab 切换** | ☐ | |
| S003 | 外观Tab | **Tab 切换** | ☐ | |
| S004 | API Provider | **Select 组件**<br>`v-model="form.apiType"`<br>选项: openai / deepseek / azure / anthropic / ollama | ☐ | |
| S005 | API Key | **Input.Password 组件**<br>`v-model="form.apiKey"`<br><br>**保存时**<br>`llmStore.saveConfig(form)` | ☐ | |
| S006 | API地址 | **条件显示**<br>`form.apiType === 'deepseek' \|\| form.apiType === 'ollama'`<br><br>**Input 组件**<br>`v-model="form.apiUrl"` | ☐ | |
| S007 | 模型选择 | **Select 组件**<br>`v-model="form.model"`<br><br>**点击"获取模型列表"**<br>`llmStore.fetchModels()`<br><br>**→ 调用后端 API**<br>`GET /api/llm/models`<br>响应: `{ models: [{ id, name }] }`<br><br>**→ Store 更新**<br>`llmStore.availableModels = models.map(m => m.id)` | ☐ | |
| S008 | Thinking模式 | **Checkbox 组件**<br>`v-model="form.thinking"` | ☐ | |
| S009 | 测试连接 | **点击**<br>`isTesting.value = true`<br><br>**→ 先保存配置**<br>`llmStore.saveConfig(form)` → `POST /api/llm/config`<br><br>**→ 调用后端 API**<br>`llmStore.testConnection()` → `POST /api/llm/test`<br>请求体: `{ apiType, apiKey, apiUrl, model, thinking }`<br><br>**→ Store 更新**<br>`success` → `llmStore.isConnected = true`<br>`fail` → `llmStore.isConnected = false`<br><br>**→ 通知**<br>`notification.success('连接成功')`<br>`notification.error(errorMessage)` | ☐ | |
| S010 | 获取模型列表 | 同 S007 | ☐ | |
| S011 | 自动化等级 | **Radio.Group**<br>`L1 半自动(每步需确认)` / `L2 连续(自动连续执行)`<br><br>**保存到 localStorage**<br>`localStorage.setItem('moyun-auto-mode', value)`<br><br>**同步到后端**<br>`saveRemoteConfig({ autoMode: value })` | ☐ | |
| S012 | 主题选择 | **Radio.Button.Group**<br>三个主题选项<br><br>**点击**<br>`uiStore.setTheme(theme)`<br><br>**→ 应用主题**<br>`document.documentElement.dataset.theme = theme === 'dark' ? '' : theme`<br><br>**→ 保存到 localStorage (persist)** | ☐ | |
| S013 | 保存设置 | **点击确定**<br><br>**→ 调用后端 API**<br>`llmStore.saveConfig(form)`<br><br>**→ 通知**<br>`notification.success('设置已保存')`<br><br>**→ 关闭 Modal**<br>`uiStore.closeSettings()` | ☐ | |

### 6.4 其他模态框

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| X001 | Token统计 | 未实现（待开发） | ☐ | 对应 功能清单.md G0118 |
| X002 | 内容对比 | 未实现（待开发） | ☐ | 对应 功能清单.md G0119 |
| X003 | 反馈 | 未实现（待开发） | ☐ | 对应 功能清单.md G0123 |
| X004 | 修改日志 | 未实现（待开发） | ☐ | 对应 功能清单.md G0124 |
| X005 | 批量生成 | 未实现（待开发） | ☐ | |
| X006 | 质量审查 | 未实现（待开发） | ☐ | |
| X007 | 快速打开 | 未实现（待开发） | ☐ | |
| X008 | 全局搜索 | 未实现（待开发） | ☐ | |

---

## 七、通知系统

使用 Ant Design Vue 的 `notification` API，非自定义组件。

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| N001 | 成功通知 | **调用**<br>`notification.success({ message, duration: 5 })`<br><br>**→ Ant Design 处理**<br>自动添加到通知队列，5秒后移除 | ☐ | 非自定义 NotificationContainer |
| N002 | 错误通知 | 同 N001，`notification.error(...)` | ☐ | |
| N003 | 警告通知 | 同 N001，`notification.warning(...)` | ☐ | |
| N004 | 信息通知 | 同 N001，`notification.info(...)` | ☐ | |
| N005 | 自动消失 | `duration: 5` 秒后 Ant Design 自动关闭 | ☐ | |
| N006 | 手动关闭 | 通知自带 X 关闭按钮 | ☐ | |
| N007 | 堆叠显示 | Ant Design 默认行为<br>右上角堆叠，间距自动处理 | ☐ | |

---

## 八、布局功能 (AppLayout.vue)

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| L001 | 三栏布局 | **CSS Flexbox**<br>`display: flex`<br>左栏: `width: leftWidth%`<br>中栏: `flex: 1`<br>右栏: `width: rightWidth%` | ☐ | |
| L002 | 左栏宽度拖拽 | **鼠标事件**<br>`mousedown` → `startHDrag(e)`<br>`mousemove` → `onHDrag(e)`<br>`mouseup` → `stopDrag()`<br><br>**计算**<br>`newWidth = (clientX - containerLeft) / containerWidth * 100`<br>限制: `10 <= newWidth <= 60`<br><br>**→ 保存**<br>`localStorage.setItem('moyun-layout-sizes', JSON.stringify({ leftWidth, rightWidth, editorChat }))`<br>`saveRemoteConfig({ layout: { leftWidth, rightWidth, editorChat } })` | ☐ | |
| L003 | 右栏宽度拖拽 | 同 L002 | ☐ | |
| L004 | 编辑器/聊天分隔 | 同 L002，但调整上下比例<br>限制: `15 <= editorChat <= 85` | ☐ | |
| L005 | 布局持久化 | **onMounted**<br>`const saved = localStorage.getItem('moyun-layout-sizes')`<br>应用保存的宽度值 | ☐ | |
| L006 | 布局同步 | **调用**<br>`saveRemoteConfig({ layout: {...} })`<br><br>**→ 后端 API**<br>`POST /api/config`<br>保存到 `.config.json` | ☐ | |

---

## 九、全局功能 (App.vue)

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| G001 | 应用初始化 | **onMounted**<br>`initApp()`<br><br>**→ 加载配置**<br>`llmStore.loadConfig()` → `GET /api/llm/config`<br><br>**→ 测试连接**<br>`llmStore.testConnection()` → `POST /api/llm/test`<br><br>**→ 连接 SSE**<br>`sseService.connect()`<br><br>**→ 加载项目列表**<br>`projectStore.loadProjects()` → `GET /api/projects` | ☐ | |
| G002 | URL恢复 | **onMounted**<br>检查 `route.params`<br><br>**→ 有 projectId**<br>`projectStore.openProject(projectId)`<br>`fileStore.loadTree(projectId)`<br><br>**→ 有 pathMatch**<br>`editorStore.setCurrentFile(path)` | ☐ | |
| G003 | 离开确认 | **window.beforeunload**<br>`if (editorStore.isDirty) {`<br>`  event.preventDefault()`<br>`  event.returnValue = ''`<br>`}` | ☐ | |
| G004 | 全局错误捕获 | **window.onerror**<br>`if (!message.includes('ResizeObserver')) {`<br>`  notification.error('发生错误: ' + message)`<br>`}` | ☐ | |
| G005 | Promise错误捕获 | **window.unhandledrejection**<br>`notification.error('Promise错误: ' + reason)` | ☐ | |
| G006 | 自动生成触发 | **watch**<br>`watch(() => projectStore.pendingGeneration, async (pending) => {`<br>`  if (!pending) return`<br><br>`  await router.isReady()`<br>`  await nextTick()`<br><br>`  // 打开文件`<br>`  editorStore.setFilePrompt(filePath, '')`<br>`  fileStore.openFile(node)`<br>`  editorStore.setCurrentFile(filePath)`<br><br>`  // 触发生成`<br>`  await fileGen.generateToFile(...)`<br><br>`  // 完成`<br>`  await refreshFileContent()`<br>`  projectStore.pendingGeneration = null`<br>`})` | ☐ | |
| G007 | Prompt同步 | **watch**<br>`watch(() => editorStore.currentFilePath, (path) => {`<br>`  if (path) {`<br>`    const prompt = editorStore.getFilePrompt(path)`<br>`    rightPanelStore.updatePrompt(prompt \|\| '')`<br>`  }`<br>`})` | ☐ | |

---

## 十、主题系统

| 编号 | 功能 | 完整操作流程 | 删除 | 调整/备注 |
|------|------|-------------|------|-----------|
| T001 | 水墨丹青（默认） | **`:root` CSS 变量**<br>`--ink-deepest: #0a0a0f`<br>`--ink-deep: #111118`<br>`--ink-dark: #1a1a24`<br>`--gold-primary: #c9a04e`<br>`--text-warm-white: #f0ede8`<br>兼容别名: `--bg-primary` → `--ink-deep`<br>`--accent-primary` → `--gold-primary` | ☐ | 原"深邃夜紫"已重构为水墨丹青体系 |
| T002 | 墨绿护眼 | **`[data-theme="green"]` CSS 变量**<br>覆盖 ink 和 gold 色系为暖色调<br>`--ink-deep: #161e16`<br>`--gold-primary: #8b9a6e` | ☐ | |
| T003 | 经典炭灰 | **`[data-theme="dark-gray"]` CSS 变量**<br>冷灰蓝调<br>`--ink-deep: #141418`<br>`--gold-primary: #888fa0` | ☐ | |
| T004 | 主题持久化 | **Pinia persist + localStorage**<br>保存到 `localStorage('moyun-theme')`<br>页面刷新后通过 `document.documentElement.dataset.theme` 恢复 | ☐ | |

---

## 实时事件处理

### SSE 事件 (useSSE.ts → EventSource)

通过 `EventSource` 连接 `GET /api/events` 接收：

| 事件类型 | 处理流程 |
|----------|----------|
| `file-created` | `fileStore.refreshTree()` → 刷新文件树 |
| `file-updated` | `editorStore.updateContent(path, data.content)` → 更新编辑器内容（含frontmatter） |
| `file-renamed` | `fileStore.refreshTree()` → 刷新文件树，更新打开的标签页路径 |
| `directory-created` | `fileStore.refreshTree()` → 刷新文件树 |
| `task` | `taskStore.updateTask(taskId, data)` → 更新任务状态 |
| `queue` | `taskStore.refreshQueue()` → 刷新堆栈列表 |
| `llm-status` | `llmStore.updateStatus(data)` → 更新 LLM 状态灯 |
| `thinking` | `chatStore.currentThinking = data.content` → 显示思考过程 |
| `diff_summary` | `useDiffSummary().setSummary(data.summary, data.target_file)` → AI 修改摘要 |
| `error` | `notification.error(data.message)` → 显示错误横幅 |
| `done` | `chatStore.finishAIMessage()` + `llmStore.isGenerating = false` |

### AI 生成内容 (useFileGeneration.ts → fetch + ReadableStream)

管线执行时的生成内容**不经过 SSE EventSource**，而是通过 `fetch` + `ReadableStream` 接收：

| 通道 | 处理流程 |
|------|----------|
| `generation` | `generationEmitter` 分发 CustomEvent → `editorStore.appendContent(path, data.content)` 追加到编辑器 |

---

## 删除功能填写区

如需删除以下功能，请打 ✓：

| 功能编号 | 功能名称 | 删除 ✓ |
|----------|----------|--------|
| | | ☐ |
| | | ☐ |
| | | ☐ |
| | | ☐ |
| | | ☐ |

---

## 功能调整填写区

如需调整以下功能，请填写：

| 功能编号 | 当前行为 | 期望行为 |
|----------|----------|----------|
| 例如: TB005 | 按钮文字"润色" | 改为"润色内容" |
| | | |
| | | |
| | | |
| | | |
| | | |

---

**请在「删除功能填写区」标记要删除的功能，在「功能调整填写区」填写需要调整的功能。完成后返回文档。**
