/** 候选稿模块 - 类型定义 — 核心类型已迁移到 @/shared/api/types */

export type { CandidateAction, CandidateStatus, CandidateInfo, CandidateDetail, CandidateRevisionRequest } from '@/shared/api/types'
export type { CandidateAdoptResult as AdoptResult } from '@/shared/api/types'
