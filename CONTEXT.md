# 墨韵 - 领域上下文

本文档定义墨韵项目的核心领域术语和概念。

## 核心领域术语

### 项目与结构

| 术语 | 定义 |
|------|------|
| **项目 (Project)** | 一个完整的小说创作项目，包含多个章节、角色和素材 |
| **章节 (Chapter)** | 小说的基本单位，包含正文和元数据 |
| **卷 (Volume)** | 章节的逻辑分组，用于组织长篇小说 |
| **工作区 (Workspace)** | 项目文件的根目录 |

### 创作模式

| 术语 | 定义 |
|------|------|
| **爽文模式 (Lite Mode)** | 面向普通用户的轻量创作页面，通过开局卡、爽点卡、流式生成、候选稿和故事引擎推动创作 |
| **专业模式 (Professional Mode)** | 面向进阶作者和开发者的工作台，暴露文件、Prompt、管线、工作流、变量、候选稿和运行日志 |
| **人机协同创作 (Human-AI Co-writing)** | AI 负责生成、总结、审稿和候选改写，作者负责表达意图、选择方向、确认关键状态 |
| **作者意图 (Author Intent)** | 作者通过聊天、参数、人工节点表达的创作要求，可影响当前文件、后续章节、故事状态或候选改稿 |

### 工作流节点系统

| 术语 | 定义 |
|------|------|
| **工作流 (Workflow)** | 安排完整创作任务的编排层，负责步骤顺序、暂停恢复、变量传递、错误处理和执行记录 |
| **执行组 (Chain)** | 一组紧密相关的节点，通常完成生成正文候选稿、质量审稿、记忆更新等中等粒度目标 |
| **执行节点 (Node)** | 最小可执行单元，Prompt、人工确认、文件读写、记忆更新、质量审稿都属于节点 |
| **Prompt节点 (Prompt Node)** | 由 AI 执行的节点，用于生成、总结、改写、审稿、提取 |
| **人工节点 (Human Node)** | 由用户执行的节点，用于确认、修改、选择、评分或补充指令 |
| **文件节点 (File Node)** | 由系统执行的节点，用于读取、写入、追加、创建候选稿、采用候选稿、快照等文件操作 |
| **记忆节点 (Memory Node)** | 更新故事引擎、近期上下文、角色状态、伏笔等长期连续性信息的节点 |
| **质量节点 (Quality Node)** | 对生成内容进行审稿、评分、补强判断和质量摘要的节点 |
| **候选稿 (Candidate Draft)** | AI 生成但尚未覆盖正式文件的候选内容，用户采用后才写入正式文件 |
| **变量池 (Variable Pool)** | 工作流运行时保存用户输入、AI 输出、人工确认结果和系统变量的上下文集合 |

### Prompt系统

| 术语 | 定义 |
|------|------|
| **模板 (Template)** | Jinja2格式的Prompt模板文件 |
| **生成类模板 (Generate)** | 用于创建新内容的模板（书名、大纲、章节等） |
| **提取类模板 (Extract)** | 用于从内容中提取信息的模板（角色、情节等） |
| **转换类模板 (Transform)** | 用于修改现有内容的模板（润色、翻译等） |
| **片段引用 (@{path})** | 模板中引用其他文件内容的语法 |
| **管线 (Pipeline)** | YAML定义的多步骤LLM调用链，支持步骤级fallback |
| **共享块 (Block)** | 公共规则片段，用`{% include %}`跨prompt引用，改一处处处生效 |
| **增量提取 (Incremental Extract)** | 基于已有数据只输出新增内容的提取模式，与完整提取互补 |
| **修改摘要 (Diff Summary)** | 管线末尾步骤，对比原文和修改结果输出结构化分析报告 |
| **指令优先级 (Priority Hierarchy)** | chat管线中用户指令与文风指南的冲突解决规则 |

### 质量保障

| 术语 | 定义 |
|------|------|
| **文风指南 (Style Guide)** | 定义写作风格、禁忌、样本文本 |
| **故事状态 (Story State)** | 当前故事进度、伏笔、阵营等 |
| **近期上下文 (Recent Context)** | 最近几章的摘要和统计 |
| **用户反馈 (Feedback)** | 用户对生成内容的反馈 |
| **修改日志 (Revision Log)** | 内容的修改历史 |

### LLM交互

| 术语 | 定义 |
|------|------|
| **任务 (Task)** | 一次LLM调用请求，包含模板和变量 |
| **Token计数** | 使用tiktoken计算文本token数 |
| **流式输出** | LLM的增量输出方式 |
| **重试机制** | 使用tenacity的指数退避策略 |

### 版本管理

| 术语 | 定义 |
|------|------|
| **快照 (Snapshot)** | 某一时间点的文件内容副本 |
| **对比 (Diff)** | 两个版本之间的差异 |

## 服务模块

| 术语 | 定义 |
|------|------|
| **LLM服务 (LLMService)** | 封装LiteLLM调用，提供流式输出和重试 |
| **文件服务 (FileService)** | 异步文件读写，支持frontmatter |
| **Prompt引擎 (PromptEngine)** | 模板加载和渲染 |
| **任务队列 (TaskQueue)** | 管理异步生成任务 |
| **文件监听器 (FileWatcher)** | 监听文件系统变化，推送SSE事件 |

## 数据流

```
用户请求 → API路由 → Prompt引擎(渲染模板) → LLM服务(调用) → 文件服务(保存)
                                        ↓
                              Token计数(检查上下文)
```
