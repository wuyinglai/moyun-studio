/** 候选稿模块 - 统一导出 */

export type { CandidateAction, CandidateStatus, CandidateInfo, CandidateDetail, AdoptResult } from './types'
export { listCandidates, getCandidateDetail, adoptCandidate, deleteCandidate } from './api'
